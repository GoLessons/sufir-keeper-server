package api

import (
	apitypes "github.com/GoLessons/sufir-keeper-server/internal/api/types"
)

const BearerAuthScopes = apitypes.BearerAuthScopes

type (
	GetItemsParams   = apitypes.GetItemsParams
	UploadFileParams = apitypes.UploadFileParams
)
